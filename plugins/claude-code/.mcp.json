{
    "mcpServers": {
        "remindb": {
            "command": "remindb",
            "args": [
                "bridge"
            ],
            "env": {
                "REMINDB_DB": "${REMINDB_DB}",
                "REMINDB_SOURCE": "${REMINDB_SOURCE}",
                "REMINDB_RESCAN_INTERVAL": "${REMINDB_RESCAN_INTERVAL}"
            }
        }
    }
}
